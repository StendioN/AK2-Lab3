List of keys

    -l --list
    -h --help
    -v --version
    -V --value (optional parameter)	
    	-v1
    	--value=1

Makefile

makes build and run with -l -h -v -V1 keys

make build

builds app

make run

runs app
